name: Deploy Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2 Production Server
        uses: appleboy/ssh-action@v1.0.3

        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22

          script: |
            set -e

            echo "🚀 Starting PRODUCTION deployment..."

            cd /home/ubuntu

            if [ ! -d "webapplication" ]; then
              git clone https://github.com/vijayqadevkraft/webapplication.git
            fi

            cd webapplication

            git fetch origin
            git reset --hard origin/main

            # Backend setup
            cd app/backend

            node -v || sudo apt install nodejs npm -y
            npm install

            pm2 delete backend || true
            pm2 start server.js --name backend

            # Frontend deployment
            cd ../frontend

            sudo rm -rf /var/www/prod/*
            sudo cp -r . /var/www/prod/

            sudo systemctl restart nginx

            echo "✅ PRODUCTION deployment completed"
